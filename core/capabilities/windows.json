{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-chrome",
  "description": "Windows-only: invoke permissions for custom window chrome (min / max-restore / close + isMaximized state). Mac never receives these — its traffic light is native. Event listening (onResized / onFocusChanged) is granted by core:event:default which `core:default` in default.json already includes — no separate per-event permission exists in Tauri 2 stable.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close"
  ]
}
